# Small diagnostic workspace

The current health is not written in this document. To obtain it, locate the
Python status reporter under `scripts` and run it. The active reporter has
`status` in its filename; the archive reporter is only a historical example.
